<div class="dialog dialog--confirmation">

	<div class="dialog__screen">

		<div class="dialog__title">
			{{ __('general.confirmationTitle') }}
		</div>
		<div class="dialog__body">
			Body Placeholder
		</div>
		<div class="dialog__actions">
			<button class="btn btn-danger dialog__button" data-confirm="true">{{ __( 'general.yes' ) }}</button>
			<button class="btn btn-default dialog__button" data-confirm="false">{{ __( 'general.no' ) }}</button>
		</div>

	</div>

</div>
